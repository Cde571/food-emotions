---
// [slug].astro
import BlogLayout from './[slug].astro';

const { frontmatter, Content } = await import(`../../content/posts/${Astro.params.slug}.md`);

const title = frontmatter.title;
const date = frontmatter.date;
---

<BlogLayout title={title} date={date}>
  <Content />
</BlogLayout>
