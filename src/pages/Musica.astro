---
import Layout from "../layouts/Layout.astro";
import ChatbotWidget from "../components/ChatbotWidget.astro";

const defaultSongs = [
  { title: "I Like The Way You Kiss Me", artist: "Artemas" },
  { title: "Houdini", artist: "Dua Lipa" },
  { title: "Strangers", artist: "Kenya Grace" },
  { title: "Overdrive", artist: "Ofenbach" },
  { title: "My Oh My", artist: "Ava Max" },
];

const YOUTUBE_API_KEY = import.meta.env.PUBLIC_YOUTUBE_API_KEY;

// Música ambiente tipo Spotify
const ambientMusic = {
  happy: {
    name: "Alegre ☀️",
    url: "https://cdn.pixabay.com/audio/2022/03/10/audio_5ecb04070e.mp3",
    description: "Para momentos alegres y llenos de energía positiva"
  },
  calm: {
    name: "Tranquilo 🌙",
    url: "https://cdn.pixabay.com/audio/2022/03/24/audio_c52ef58cf2.mp3",
    description: "Música relajante para meditar o estudiar"
  },
  energetic: {
    name: "Energético ⚡",
    url: "https://cdn.pixabay.com/audio/2022/05/27/audio_1808fbf07a.mp3",
    description: "Perfecta para entrenar o actividades intensas"
  },
  focus: {
    name: "Concentración 🎯",
    url: "https://cdn.pixabay.com/audio/2022/08/02/audio_884fe92c21.mp3",
    description: "Ideal para trabajar o concentrarte"
  }
};
---

<Layout title="Música">
  <section class="pt-20 min-h-screen flex text-white bg-gradient-to-br from-[#425638] via-[#516243] to-[#687450]">
    
    <!-- 🎵 SIDEBAR - Playlists y Ambiente -->
    <aside class="w-80 bg-[#ffffff0d] backdrop-blur-md p-6 overflow-y-auto border-r border-[#a0b38b]/30 shadow-xl">
      
      <!-- Sección de música ambiente -->
      <div class="mb-8">
        <h2 class="text-xl font-bold mb-4 flex items-center gap-2 text-[#eaf0e3]">
          <span>🎭</span> Ambiente Musical
        </h2>
        <p class="text-xs text-[#dce5d3] mb-4">Música relajante de fondo mientras navegas</p>
        
        <div class="space-y-2">
          {Object.entries(ambientMusic).map(([key, mood]) => (
            <button
              class="ambient-btn w-full text-left p-3 rounded-lg bg-white/5 hover:bg-white/10 transition border border-[#a0b38b]/20 hover:border-[#a0b38b]/40"
              data-mood={key}
              data-url={mood.url}
            >
              <div class="font-semibold text-sm text-[#f3f7ef]">{mood.name}</div>
              <div class="text-xs text-[#dce5d3] mt-1">{mood.description}</div>
            </button>
          ))}
        </div>

        <div id="ambientPlayer" class="mt-4 p-3 rounded-lg bg-[#425638]/50 border border-[#a0b38b]/30 hidden">
          <div class="flex items-center justify-between mb-2">
            <div class="flex items-center gap-2">
              <button id="ambientPlayPause" class="text-xl hover:scale-110 transition">⏸️</button>
              <div>
                <div class="text-xs text-[#dce5d3]">Reproduciendo</div>
                <div id="ambientMoodName" class="text-sm font-semibold text-[#f3f7ef]"></div>
              </div>
            </div>
          </div>
          <div class="flex items-center gap-2">
            <span class="text-xs">🔊</span>
            <input 
              id="ambientVolume" 
              type="range" 
              min="0" 
              max="100" 
              value="20" 
              class="flex-1 accent-[#a0b38b]"
            />
          </div>
        </div>
      </div>

      <div class="border-t border-white/10 pt-6">
        <h2 class="text-xl font-bold mb-4 flex items-center gap-2 text-[#eaf0e3]">
          <span>📚</span> Mis playlists
        </h2>
        
        <div class="mb-4">
          <input
            id="newPlaylistInput"
            type="text"
            placeholder="Nueva playlist..."
            class="w-full p-2 rounded-lg bg-white/10 placeholder-[#cdd4c9] outline-none focus:ring-2 focus:ring-[#a0b38b] transition text-sm"
          />
          <button
            id="createPlaylistBtn"
            class="w-full mt-2 bg-gradient-to-r from-[#687450] to-[#425638] px-4 py-2 rounded-lg hover:scale-105 transition font-semibold text-[#f3f7ef] shadow-lg"
          >
            + Crear playlist
          </button>
        </div>
      </div>

      <div id="playlistsList" class="space-y-2"></div>
    </aside>

    <!-- 🎧 CONTENIDO PRINCIPAL -->
    <main class="flex-1 flex flex-col px-6 py-10 overflow-y-auto">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold">
          🎧 <span class="text-[#cddfc2]">Music Therapy</span>
        </h1>
        <p class="mt-3 text-[#e3e9dd] max-w-2xl mx-auto">
          Deja que <strong>Roy</strong> te acompañe con melodías que armonizan tu mente y cuerpo.<br />
          Busca, elige y disfruta música directamente desde YouTube.
        </p>
      </div>

      <div class="flex justify-center w-full mb-6">
        <input
          id="searchInput"
          type="text"
          placeholder="Buscar canción en YouTube..."
          class="w-11/12 sm:w-2/3 md:w-1/2 p-3 rounded-xl bg-white/10 backdrop-blur-md text-center placeholder-[#cdd4c9] outline-none focus:ring-2 focus:ring-[#a0b38b] transition"
        />
      </div>

      <div id="searchResults" class="w-full max-w-3xl mx-auto space-y-3 mb-8 hidden"></div>

      <div id="defaultList" class="w-full max-w-3xl mx-auto space-y-3">
        {defaultSongs.map((song) => (
          <div
            class="song-item flex justify-between items-center bg-white/10 backdrop-blur-md p-4 rounded-xl hover:bg-white/20 transition cursor-pointer border border-[#a0b38b]/20 hover:border-[#a0b38b]/40 shadow-md"
            data-title={song.title}
            data-artist={song.artist}
          >
            <div>
              <h3 class="text-lg font-semibold text-[#f3f7ef]">{song.title}</h3>
              <p class="text-sm text-[#dce5d3]">{song.artist}</p>
            </div>
            <button
              class="btn-play bg-gradient-to-r from-[#687450] to-[#425638] px-3 py-2 rounded-lg hover:scale-110 transition shadow-lg"
            >
              ▶
            </button>
          </div>
        ))}
      </div>

      <footer class="mt-auto pt-10">
        <div class="bg-[#ffffff0d] backdrop-blur-md rounded-2xl px-6 py-4 w-full max-w-3xl mx-auto flex flex-col items-center space-y-3 border border-[#a0b38b]/30 shadow-xl">
          <div class="text-center">
            <h3 id="now-title" class="font-semibold text-lg text-[#f3f7ef]">
              Selecciona una canción
            </h3>
            <p id="now-artist" class="text-sm text-[#cdd4c9]"></p>
          </div>

          <div id="playerContainer" class="w-full aspect-video rounded-xl overflow-hidden hidden bg-black shadow-2xl"></div>

          <div class="flex justify-center space-x-6 text-3xl">
            <button id="prevBtn" class="hover:text-[#a0b38b] transition disabled:opacity-50 disabled:cursor-not-allowed hover:scale-110" title="Anterior">⏮️</button>
            <button id="pauseBtn" class="hover:text-[#a0b38b] transition disabled:opacity-50 disabled:cursor-not-allowed hover:scale-110" title="Pausar">⏸️</button>
            <button id="playBtn" class="hover:text-[#a0b38b] transition disabled:opacity-50 disabled:cursor-not-allowed hover:scale-110" title="Reproducir">▶️</button>
            <button id="nextBtn" class="hover:text-[#a0b38b] transition disabled:opacity-50 disabled:cursor-not-allowed hover:scale-110" title="Siguiente">⏭️</button>
          </div>
        </div>
      </footer>
    </main>
  </section>

  <script define:vars={{ YOUTUBE_API_KEY }}>
    let player = null;
    let currentPlaylist = [];
    let currentIndex = 0;
    let isAPIReady = false;
    let playlists = [];
    let currentPlaylistId = null;

    // Control de música ambiente
    let ambientAudio = null;
    let currentAmbientMood = null;

    function loadYouTubeAPI() {
      if (window.YT) {
        isAPIReady = true;
        return;
      }
      const tag = document.createElement('script');
      tag.src = "https://www.youtube.com/iframe_api";
      const firstScriptTag = document.getElementsByTagName('script')[0];
      firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);
    }

    window.onYouTubeIframeAPIReady = function() {
      console.log('YouTube API Ready');
      isAPIReady = true;
    };

    function loadPlaylists() {
      const saved = JSON.parse(localStorage.getItem('musicPlaylists') || '[]');
      playlists = saved;
      renderPlaylists();
    }

    function savePlaylists() {
      localStorage.setItem('musicPlaylists', JSON.stringify(playlists));
    }

    function renderPlaylists() {
      const container = document.getElementById('playlistsList');
      
      if (playlists.length === 0) {
        container.innerHTML = '<p class="text-gray-400 text-sm text-center">No hay playlists aún</p>';
        return;
      }

      container.innerHTML = playlists.map((pl) => `
        <div class="playlist-item bg-white/5 hover:bg-white/10 rounded-lg p-3 cursor-pointer transition shadow-md ${currentPlaylistId === pl.id ? 'ring-2 ring-[#a0b38b]' : ''}" data-id="${pl.id}">
          <div class="flex justify-between items-start mb-2">
            <div class="flex-1">
              <h3 class="font-semibold text-sm text-[#f3f7ef]">${pl.name}</h3>
              <p class="text-xs text-[#dce5d3]">${pl.songs.length} canciones</p>
            </div>
            <button class="delete-playlist text-red-400 hover:text-red-300 text-xs" data-id="${pl.id}">🗑️</button>
          </div>
          <div class="space-y-1 max-h-40 overflow-y-auto">
            ${pl.songs.length === 0 ? '<p class="text-xs text-gray-500 italic">Vacía</p>' : 
              pl.songs.map((song, idx) => `
                <div class="flex items-center justify-between text-xs bg-black/20 rounded p-1 hover:bg-black/30 transition group">
                  <span class="truncate flex-1">${idx + 1}. ${song.title}</span>
                  <div class="flex gap-1 opacity-0 group-hover:opacity-100 transition">
                    <button class="play-song-btn hover:text-[#a0b38b]" data-playlist-id="${pl.id}" data-song-index="${idx}">▶️</button>
                    <button class="remove-song-btn hover:text-red-400" data-playlist-id="${pl.id}" data-song-index="${idx}">✕</button>
                  </div>
                </div>
              `).join('')
            }
          </div>
        </div>
      `).join('');

      document.querySelectorAll('.playlist-item').forEach(item => {
        item.addEventListener('click', (e) => {
          if (!e.target.closest('.delete-playlist') && !e.target.closest('.play-song-btn') && !e.target.closest('.remove-song-btn')) {
            const id = item.dataset.id;
            loadPlaylist(id);
          }
        });
      });

      document.querySelectorAll('.delete-playlist').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const id = btn.dataset.id;
          if (confirm('¿Eliminar esta playlist?')) {
            playlists = playlists.filter(pl => pl.id !== id);
            savePlaylists();
            renderPlaylists();
            if (currentPlaylistId === id) {
              currentPlaylistId = null;
            }
          }
        });
      });

      document.querySelectorAll('.play-song-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const playlistId = btn.dataset.playlistId;
          const songIndex = parseInt(btn.dataset.songIndex);
          playSongFromPlaylist(playlistId, songIndex);
        });
      });

      document.querySelectorAll('.remove-song-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const playlistId = btn.dataset.playlistId;
          const songIndex = parseInt(btn.dataset.songIndex);
          const playlist = playlists.find(pl => pl.id === playlistId);
          if (playlist) {
            playlist.songs.splice(songIndex, 1);
            savePlaylists();
            renderPlaylists();
          }
        });
      });
    }

    function loadPlaylist(id) {
      const playlist = playlists.find(pl => pl.id === id);
      if (!playlist || playlist.songs.length === 0) return;
      
      currentPlaylistId = id;
      currentPlaylist = playlist.songs;
      currentIndex = 0;
      renderPlaylists();
      playVideoByIndex(0);
    }

    function playSongFromPlaylist(playlistId, songIndex) {
      const playlist = playlists.find(pl => pl.id === playlistId);
      if (!playlist) return;
      
      currentPlaylistId = playlistId;
      currentPlaylist = playlist.songs;
      currentIndex = songIndex;
      renderPlaylists();
      playVideoByIndex(songIndex);
    }

    function addToPlaylist(song) {
      if (playlists.length === 0) {
        alert('Primero crea una playlist');
        return;
      }

      const playlistNames = playlists.map((pl, i) => `${i + 1}. ${pl.name}`).join('\n');
      const selection = prompt(`Selecciona una playlist:\n\n${playlistNames}\n\nEscribe el número:`);
      
      if (selection) {
        const index = parseInt(selection) - 1;
        if (index >= 0 && index < playlists.length) {
          playlists[index].songs.push(song);
          savePlaylists();
          renderPlaylists();
          alert(`Agregado a "${playlists[index].name}"`);
        }
      }
    }

    // Función para reproducir música ambiente
    function playAmbient(url, moodName) {
      const playerDiv = document.getElementById('ambientPlayer');
      const moodNameDiv = document.getElementById('ambientMoodName');
      const playPauseBtn = document.getElementById('ambientPlayPause');
      
      if (ambientAudio) {
        ambientAudio.pause();
        ambientAudio = null;
      }

      ambientAudio = new Audio(url);
      ambientAudio.loop = true;
      ambientAudio.volume = document.getElementById('ambientVolume').value / 100;
      
      ambientAudio.play().then(() => {
        playerDiv.classList.remove('hidden');
        moodNameDiv.textContent = moodName;
        playPauseBtn.textContent = '⏸️';
        currentAmbientMood = moodName;
      }).catch(err => {
        console.error('Error al reproducir música ambiente:', err);
        alert('No se pudo reproducir la música ambiente. Intenta de nuevo.');
      });
    }

    document.addEventListener("DOMContentLoaded", () => {
      const API_KEY = YOUTUBE_API_KEY;
      const searchInput = document.getElementById("searchInput");
      const searchResults = document.getElementById("searchResults");
      const defaultList = document.getElementById("defaultList");
      const playerContainer = document.getElementById("playerContainer");
      const nowTitle = document.getElementById("now-title");
      const nowArtist = document.getElementById("now-artist");
      const playBtn = document.getElementById("playBtn");
      const pauseBtn = document.getElementById("pauseBtn");
      const prevBtn = document.getElementById("prevBtn");
      const nextBtn = document.getElementById("nextBtn");
      const newPlaylistInput = document.getElementById("newPlaylistInput");
      const createPlaylistBtn = document.getElementById("createPlaylistBtn");

      loadYouTubeAPI();
      loadPlaylists();

      // Botones de música ambiente
      document.querySelectorAll('.ambient-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.ambient-btn').forEach(b => {
            b.classList.remove('ring-2', 'ring-[#a0b38b]', 'bg-white/15');
          });
          btn.classList.add('ring-2', 'ring-[#a0b38b]', 'bg-white/15');
          
          const url = btn.dataset.url;
          const mood = btn.querySelector('.font-semibold').textContent;
          playAmbient(url, mood);
        });
      });

      // Control de volumen ambiente
      document.getElementById('ambientVolume')?.addEventListener('input', (e) => {
        if (ambientAudio) {
          ambientAudio.volume = e.target.value / 100;
        }
      });

      // Play/Pause ambiente
      document.getElementById('ambientPlayPause')?.addEventListener('click', () => {
        if (ambientAudio) {
          if (ambientAudio.paused) {
            ambientAudio.play();
            document.getElementById('ambientPlayPause').textContent = '⏸️';
          } else {
            ambientAudio.pause();
            document.getElementById('ambientPlayPause').textContent = '▶️';
          }
        }
      });

      createPlaylistBtn.addEventListener('click', () => {
        const name = newPlaylistInput.value.trim();
        if (!name) {
          alert('Escribe un nombre para la playlist');
          return;
        }
        
        playlists.push({
          id: Date.now().toString(),
          name: name,
          songs: []
        });
        
        savePlaylists();
        renderPlaylists();
        newPlaylistInput.value = '';
      });

      newPlaylistInput.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') {
          createPlaylistBtn.click();
        }
      });

      function updateButtons() {
        prevBtn.disabled = currentIndex === 0;
        nextBtn.disabled = currentIndex >= currentPlaylist.length - 1;
      }

      async function searchYouTube(query) {
        if (!query) return;
        defaultList.classList.add("hidden");
        searchResults.classList.remove("hidden");
        searchResults.innerHTML = "<p class='text-center text-gray-400'>Buscando...</p>";

        try {
          const url = `https://www.googleapis.com/youtube/v3/search?part=snippet&type=video&videoCategoryId=10&maxResults=6&q=${encodeURIComponent(query)}&key=${API_KEY}`;
          
          const res = await fetch(url);
          
          if (!res.ok) {
            const errorData = await res.json();
            console.error('Error de API:', errorData);
            searchResults.innerHTML = `<p class='text-red-400 text-center'>Error: ${errorData.error?.message || 'Error al buscar'}</p>`;
            return;
          }

          const data = await res.json();

          if (!data.items || data.items.length === 0) {
            searchResults.innerHTML = "<p class='text-center text-gray-400'>Sin resultados.</p>";
            return;
          }

          currentPlaylist = data.items.map(v => ({
            id: v.id.videoId,
            title: v.snippet.title,
            channel: v.snippet.channelTitle
          }));

          searchResults.innerHTML = currentPlaylist
            .map((v, index) => `
              <div class="result-item flex items-center bg-white/10 backdrop-blur-md p-3 rounded-xl hover:bg-white/20 transition shadow-md border border-[#a0b38b]/20" data-index="${index}">
                <img src="https://i.ytimg.com/vi/${v.id}/mqdefault.jpg" class="w-24 h-16 rounded-lg mr-4 object-cover shadow-lg"/>
                <div class="flex-1">
                  <h3 class="text-base font-semibold line-clamp-2 text-[#f3f7ef]">${v.title}</h3>
                  <p class="text-sm text-[#dce5d3]">${v.channel}</p>
                </div>
                <div class="flex gap-2">
                  <button class="add-to-playlist-btn bg-[#687450] hover:bg-[#7a8660] px-3 py-2 rounded-lg transition text-sm shadow-md" data-index="${index}" title="Agregar a playlist">➕</button>
                  <button class="play-result-btn bg-gradient-to-r from-[#687450] to-[#425638] px-3 py-2 rounded-lg hover:scale-110 transition shadow-lg">▶</button>
                </div>
              </div>`)
            .join("");

          document.querySelectorAll(".result-item").forEach((item) => {
            const playBtn = item.querySelector(".play-result-btn");
            const addBtn = item.querySelector(".add-to-playlist-btn");
            
            playBtn.addEventListener("click", (e) => {
              e.stopPropagation();
              const index = parseInt(item.dataset.index);
              playVideoByIndex(index);
            });

            addBtn.addEventListener("click", (e) => {
              e.stopPropagation();
              const index = parseInt(item.dataset.index);
              addToPlaylist(currentPlaylist[index]);
            });
          });
        } catch (err) {
          console.error('Error de búsqueda:', err);
          searchResults.innerHTML = "<p class='text-red-400 text-center'>Error de conexión.</p>";
        }
      }

      function playVideoByIndex(index) {
        if (index < 0 || index >= currentPlaylist.length) return;
        currentIndex = index;
        const video = currentPlaylist[index];
        playVideo(video.id, video.title, video.channel);
        updateButtons();
      }

      function playVideo(videoId, title, channel = "YouTube Music") {
        playerContainer.classList.remove("hidden");
        nowTitle.textContent = title;
        nowArtist.textContent = channel;

        const initPlayer = () => {
          if (player && player.loadVideoById) {
            player.loadVideoById(videoId);
          } else {
            player = new YT.Player('playerContainer', {
              videoId: videoId,
              playerVars: {
                autoplay: 1,
                controls: 1,
                modestbranding: 1,
                rel: 0,
              },
              events: {
                'onStateChange': onPlayerStateChange
              }
            });
          }
        };

        if (isAPIReady) {
          initPlayer();
        } else {
          setTimeout(() => {
            if (isAPIReady) {
              initPlayer();
            }
          }, 1000);
        }
      }

      function onPlayerStateChange(event) {
        if (event.data === YT.PlayerState.ENDED) {
          if (currentIndex < currentPlaylist.length - 1) {
            playVideoByIndex(currentIndex + 1);
          }
        }
      }

      searchInput.addEventListener("keydown", (e) => {
        if (e.key === "Enter") {
          const value = e.target.value.trim();
          if (value) searchYouTube(value);
        }
      });

      document.querySelectorAll(".song-item").forEach((item) => {
        const title = item.dataset.title;
        const artist = item.dataset.artist;
        const btn = item.querySelector(".btn-play");

        btn.addEventListener("click", (e) => {
          e.stopPropagation();
          searchYouTube(`${title} ${artist}`);
        });
      });

      pauseBtn.addEventListener("click", () => {
        if (player && player.pauseVideo) {
          player.pauseVideo();
        }
      });
      
      playBtn.addEventListener("click", () => {
        if (player && player.playVideo) {
          player.playVideo();
        }
      });

      prevBtn.addEventListener("click", () => {
        if (currentIndex > 0) {
          playVideoByIndex(currentIndex - 1);
        }
      });

      nextBtn.addEventListener("click", () => {
        if (currentIndex < currentPlaylist.length - 1) {
          playVideoByIndex(currentIndex + 1);
        }
      });

      updateButtons();
    });
  </script>
  <ChatbotWidget />
</Layout>


